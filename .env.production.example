# QMS Production Environment Variables
# AWS Lightsail Ubuntu 24.04

# Usage:
# 1. Copy this file: cp .env.production.example .env
# 2. Update the values below for your environment
# 3. Never commit .env file to Git!

# ==================== Django Settings ====================

# Django Secret Key (REQUIRED - CHANGE THIS!)
# Generate: cd backend && python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-production-secret-key-here-change-this-immediately

# Debug Mode (MUST be False in production)
DEBUG=False

# Allowed Hosts (comma-separated domains or IPs)
# Example: ALLOWED_HOSTS=example.com,www.example.com,123.456.789.0
ALLOWED_HOSTS=your-domain.com,www.your-domain.com

# Frontend URL
# Example: FRONTEND_URL=https://your-domain.com
FRONTEND_URL=https://your-domain.com

# CSRF Trusted Origins (required for domains)
# Example: CSRF_TRUSTED_ORIGINS=https://your-domain.com,https://www.your-domain.com
CSRF_TRUSTED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# ==================== Database Settings ====================

# SQLite database filename
DATABASE_NAME=db.sqlite3

# ==================== Static Files Settings ====================

STATIC_URL=/static/
STATIC_ROOT=staticfiles

# ==================== Security Settings (HTTPS) ====================

# HTTPS Redirect (set to True after SSL certificate installation)
SECURE_SSL_REDIRECT=True

# Secure Cookies (set to True in HTTPS environment)
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True

# ==================== JWT Token Settings ====================

# Access Token lifetime (hours)
ACCESS_TOKEN_LIFETIME_HOURS=12

# Refresh Token lifetime (days)
REFRESH_TOKEN_LIFETIME_DAYS=7

# ==================== AWS S3 Backup Settings (Optional) ====================

# S3 Bucket Name
AWS_S3_BACKUP_BUCKET=your-qms-backup-bucket

# AWS Credentials (IAM User)
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# AWS Region
AWS_DEFAULT_REGION=ap-northeast-2

# ==================== Backup Settings ====================

# Maximum local backup files to keep
MAX_BACKUP_FILES=7

# ==================== Frontend API URL ====================

# API URL used by Next.js frontend
NEXT_PUBLIC_API_URL=https://your-domain.com/api
